import React from "react";
import { Box, Drawer } from "@mui/material";

export const DiscursiveQuestions: React.FC = () => {
    return(
        <React.Fragment>
            <Drawer>

            </Drawer>

            
        </React.Fragment>
    )
}